# เปิดเว็บดูตอนพัฒนา

```bash
npm install
npm run dev
```

เปิด http://localhost:3000

## ทำไมต้องมีไฟล์ serve.json

`serve` เปิดความสามารถ **cleanUrls** มาให้โดยปริยาย ซึ่งจะเปลี่ยนเส้นทาง
`/leave-request-detail.html?id=lr001` ไปเป็น `/leave-request-detail`
แล้ว **ตัด `?id=lr001` ทิ้ง**

ผลคือกดแถวในตารางแล้วจะขึ้นว่า "ไม่พบใบขอลาที่ต้องการ" ทุกแถว
ทั้งที่โค้ดถูกต้อง เพราะหน้ารายละเอียดอ่านรหัสใบลาจากท้าย URL

`serve.json` ปิดความสามารถนี้ `serve` อ่านไฟล์นี้เองอัตโนมัติ ไม่ต้องสั่งอะไรเพิ่ม

## ถ้าแก้แล้วยังพังเหมือนเดิม

Chrome จำการเปลี่ยนเส้นทางแบบ 301 ไว้ในแคชแบบถาวร กดรีเฟรชแรง ๆ ก็ไม่หาย
เพราะเบราว์เซอร์เปลี่ยนเส้นทางเองก่อนจะถึงเซิร์ฟเวอร์ด้วยซ้ำ

วิธีที่เร็วที่สุดคือ **เปลี่ยนพอร์ต** เช่น `npx serve -l 3100 .`
คนละพอร์ตคือคนละที่อยู่ แคชเดิมจึงไม่โดน
