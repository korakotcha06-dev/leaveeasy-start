// ─────────────────────────────────────────────────────────────
// js/icons.js — ชุดไอคอนของ LeaveEasy
//
// ทำไมเป็น SVG เขียนไว้ในไฟล์นี้เอง ไม่ใช่ไลบรารีจากข้างนอก
//   - ใบงานห้ามใช้ framework และห้ามมีขั้นตอน build
//   - ไม่ต้องโหลดไฟล์จากเน็ต เว็บจึงไม่พังตอนออฟไลน์
//   - เปลี่ยนสีตามตัวอักษรรอบข้างได้เอง เพราะใช้ currentColor
//
// วิธีใช้ 2 แบบ
//   1) ใน HTML ธรรมดา  <span data-icon="user"></span>
//   2) ใน JavaScript    ไอคอน("user")   คืนค่าเป็นข้อความ <svg>
//
// ไอคอนเป็นของประดับ ไม่ใช่ข้อมูล จึงใส่ aria-hidden ไว้ทุกตัว
// ห้ามใช้ไอคอนสื่อความหมายเพียงลำพัง ต้องมีข้อความกำกับเสมอ
// ─────────────────────────────────────────────────────────────

var เส้นไอคอน = {
  // คน — ใช้แทนบทบาทผู้ขอลา
  user: '<circle cx="12" cy="8" r="4"/><path d="M4 21c0-4 3.6-6 8-6s8 2 8 6"/>',
  // ถูกในวงกลม — ผู้อนุมัติ
  "check-circle": '<circle cx="12" cy="12" r="9"/><path d="M8 12.5l2.5 2.5L16 9.5"/>',
  // เอกสาร — ฝ่ายบุคคล
  "file-text": '<path d="M14 3H7a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h10a2 2 0 0 0 2-2V8z"/>' +
               '<path d="M14 3v5h5M9 13h6M9 17h4"/>',
  // กล่องว่าง — สถานะไม่มีข้อมูล
  inbox: '<path d="M3 13h4l2 3h6l2-3h4"/>' +
         '<path d="M5 5h14l2 8v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4z"/>',
  // สามเหลี่ยมเตือน
  "alert-triangle": '<path d="M12 4L2.5 20h19z"/><path d="M12 10v4M12 17.5v.01"/>',
  // วงกลมเตือน — ข้อผิดพลาด
  "alert-circle": '<circle cx="12" cy="12" r="9"/><path d="M12 7.5v5M12 16v.01"/>',
  // ถูก — สำเร็จ
  check: '<path d="M4 12.5l5 5L20 6.5"/>',
  // ลูกศรซ้าย — ย้อนกลับ
  "arrow-left": '<path d="M19 12H5"/><path d="M11 6l-6 6 6 6"/>',
  // เครื่องหมายบวก — เพิ่มรายการ
  plus: '<path d="M12 5v14M5 12h14"/>',
  // นาฬิกา — ใบที่ยังรอพิจารณา
  clock: '<circle cx="12" cy="12" r="9"/><path d="M12 7v5l3.5 2"/>',
  // กากบาทในวงกลม — ใบที่ไม่อนุมัติ
  "x-circle": '<circle cx="12" cy="12" r="9"/><path d="M9 9l6 6M15 9l-6 6"/>',
  // ปฏิทินมีเครื่องหมายถูก — เครื่องหมายประจำระบบ
  "calendar-check": '<rect x="3" y="5" width="18" height="16" rx="2"/>' +
                    '<path d="M8 3v4M16 3v4M3 10h18"/><path d="M9 15.5l2 2 4-4"/>'
};

// คืนค่าเป็นข้อความ <svg> เอาไปต่อกับ HTML ได้เลย
function ไอคอน(ชื่อ, คลาสเพิ่ม) {
  var เส้น = เส้นไอคอน[ชื่อ];
  if (!เส้น) return "";
  return '<svg class="icon' + (คลาสเพิ่ม ? " " + คลาสเพิ่ม : "") + '" ' +
         'viewBox="0 0 24 24" aria-hidden="true" focusable="false">' + เส้น + "</svg>";
}

// เติมไอคอนให้ทุกจุดใน HTML ที่เขียน data-icon ไว้
(function เติมไอคอนในหน้า() {
  document.querySelectorAll("[data-icon]").forEach(function (ที่วาง) {
    ที่วาง.innerHTML = ไอคอน(ที่วาง.dataset.icon);
  });
})();

// ── ใส่ข้อความลงกล่องแจ้งเตือนพร้อมไอคอนที่ตรงกับชนิด ──
// ข้อความผ่าน textContent เสมอ ข้อมูลจากผู้ใช้จึงแทรกโค้ดเข้าหน้าเว็บไม่ได้
var ไอคอนของชนิด = {
  warn: "alert-triangle",
  error: "alert-circle",
  ok: "check"
};

function เตือนพร้อมไอคอน(กล่อง, ชนิด, ข้อความ) {
  กล่อง.innerHTML = ไอคอน(ไอคอนของชนิด[ชนิด] || "alert-circle");
  กล่อง.appendChild(document.createTextNode(ข้อความ));
  กล่อง.setAttribute("role", ชนิด === "ok" ? "status" : "alert");
}
